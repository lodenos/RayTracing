#include "raytracing.h"

void intersection_point(object_t *object, cl_float4 *origin,
                        cl_float4 *direction) {
  (void)object;
  (void)origin;
  (void)direction;

  // register float b;
  // register float c;
  //
  // if (c < 0)
  //   return -1;
  // c = sqrtf(c);
  // return ((-b - c > 0) ? -b - c : -b + c);
}
