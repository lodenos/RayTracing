#include "raytracing.h"

float intersection_point(object_t *object, cl_float4 *origin,
                         cl_float4 *direction) {
  (void)object;
  (void)origin;
  (void)direction;
  return 1.0f;
}
